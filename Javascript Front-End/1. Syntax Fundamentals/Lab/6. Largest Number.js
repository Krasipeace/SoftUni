function getMaxNumber(x, y, z) {
    let result = Math.max(x, y, z);
    console.log(`The largest number is ${result}.`);
}

getMaxNumber(5, -3, 16); // The largest number is 16.
getMaxNumber(-3, -5, -22.5); // The largest number is -3.